[build]
builder = "nixpacks"
buildCommand = "cd web_todo_app/backend && pip install -r requirements.txt"

[deploy]
startCommand = "cd web_todo_app/backend && uvicorn src.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
